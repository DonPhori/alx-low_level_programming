#include <stdio.h>

/**
 * main - Entry point to program
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 on success
 */

int main(int argc, char *argv[])
{
	int args, i;

	for (i = 0; i < argc; i++)
	{
		args = i;
	}
	printf("%d\n", args);

	return (0);
}
